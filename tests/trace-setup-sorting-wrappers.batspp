# Setup
$ bind 'set enable-bracketed-paste off'

## Bracketed Paste is disabled to prevent characters after output
## Example: 
# $ echo 'Hi'
# | Hi?2004l

## TEST:  Make sure simple prompt used (e.g., no escapes that might contaminate output)
## PS1="$ "
## TODO: PS1="> "
## NOTE: The Jupyter bash kernel requires that PS1 not be modified as they customize it. 
$ echo $PS1
[PEXP\[\]ECT_PROMPT>

# Delete all aliases and function
# TODO: Instead start from pristine environment
$ unalias -a
$ alias | wc -l
$ for f in $(typeset -f | egrep '^\w+'); do unset -f $f; done
$ typeset -f | egrep '^\w+' | wc -l
0
0

# Setting a temp directory for tests
$ TMP=/tmp/test-setupsort
## NOTE: Source it directly from the ./tests directory.
$ BIN_DIR=$PWD/..
$ alias | wc -l
0

## NOTE: For reproducability, the directory name needs to be fixed
## In place of $$, use a psuedo random number (e,g., 6411)
## *** All output from one run to the next needs to be the same ***
## temp_dir=$TMP/test-$$
$ temp_dir=$TMP/test-6411
$ mkdir -p "$temp_dir"
# TODO: /bin/rm -rvf "$temp_dir"
$ cd "$temp_dir"
$ pwd
#ALIAS FOR PRINTING SEPERATION LINES (FOR JUPYTER)
$ alias linebr="printf '%*s\n' "${COLUMNS:-$(tput cols)}" '' | tr ' ' -"
/tmp/test-setupsort/test-6411

# Count aliases proper
$ alias | wc -l
# Count functions
$ typeset -f | egrep '^\w+' | wc -l
1
0

## TEST 1 - SETUP AND SORTING WRAPPERS
# Editing and activating new settings
#
# ORIGINAL: alias do-setup="conditional-source $HOME/.bashrc"
$ alias bashrc-setup="~/.bashrc"
# Sorting wrappers
#
$ alias tab-sort="sort -t $'\t'"
$ alias colon-sort="sort $SORT_COL2 -t ':'"
$ alias colon-sort-rev-num='colon-sort -rn'
$ alias freq-sort='tab-sort -rn $SORT_COL2'
#
# para-sort: sort paragraphs alphabetically
# TODO: use '$@' for '$*' (or note why not appropriate)
$ function para-sort() { perl -00 -e '@paras=(); while (<>) {push(@paras,$_);} print join("\n", sort @paras);' $*; }
#
# echoize: output stdin (e.g., command output) on single line, as if echo $(command)
## BAD: alias echoize="perl -pe 's/\n(.)/ $1/;'"
## TODO: alias echoize="perl -pe 's/\n(.)/ \$1/;'"
$ function echoize { perl -00 -pe 's/\n(.)/ $1/g;'; }

# FOR SOME REASONS "cat bashrc-setup" COMMAND GIVES AN ERROR
$ cat ~/.bashrc | tab-sort
$ linebr
$ cat ~/.bashrc | colon-sort
$ linebr
$ cat ~/.bashrc | colon-sort-rev-num
$ linebr
$ cat ~/.bashrc | freq-sort
$ linebr
























    ;;
    ;;
*)
	# a case would tend to support setf rather than setaf.)
# Add an "alert" alias for long running commands.  Use like so:
alias alert='notify-send --urgency=low -i "$([ $? = 0 ] && echo terminal || echo error)" "$(history|tail -n1|sed -e '\''s/^\s*[0-9]\+\s*//;s/[;&|]\s*alert$//'\'')"'
alias cls='clear'
alias debugpp='VERBOSE=1 BATS_OPTIONS="--no-tempdir-cleanup --pretty --verbose-run --show-output-of-passing-tests" batspp'
# Alias definitions.
    #alias dir='dir --color=auto'
    alias egrep='egrep --color=auto'
    alias fgrep='fgrep --color=auto'
# ALIAS FOR BATSPP & ITS DEBUG MODE
# Alias for clearing screen
alias get-batspp="./jupyter_to_batspp.py"
    alias grep='grep --color=auto'
alias la='ls -A'
    alias ll='exa --icons -la'
alias ll='ls -alF'
alias l='ls -CF'
alias lsd='ls -d */'
    alias ls='exa --icons'
alias simple-batspp-debug='VERBOSE=1 BATS_OPTIONS="--no-tempdir-cleanup --pretty --verbose-run --show-output-of-passing-tests" simple-batspp'
alias simple-batspp="./simple_batspp.py"
#Alias to display directory
    alias tree='exa --icons --tree'
    #alias vdir='vdir --color=auto'
# append to the history file, don't overwrite it
    . ~/.bash_aliases
# ~/.bash_aliases, instead of adding them here directly.
# ~/.bashrc: executed by bash(1) for non-login shells.
case $- in
case "$TERM" in
case "$TERM" in
# check the window size after each command and, if necessary,
# colored GCC warnings and errors
	color_prompt=
	color_prompt=yes
# <<< conda initialize <<<
# >>> conda initialize >>>
__conda_setup="$('/home/aveey/miniconda3/bin/conda' 'shell.bash' 'hook' 2> /dev/null)"
# !! Contents within this block are managed by 'conda init' !!
    debian_chroot=$(cat /etc/debian_chroot)
# don't put duplicate lines or lines starting with space in the history.
  elif [ -f /etc/bash_completion ]; then
    else
    else
else
else
# enable color support of ls and also add handy aliases
# enable programmable completion features (you don't need to enable
esac
esac
esac
    . /etc/bash_completion
    eval "$__conda_setup"
#export GCC_COLORS='error=01;31:warning=01;35:note=01;36:caret=01;32:locus=01:quote=01'
        export PATH="/home/aveey/miniconda3/bin:$PATH"
    fi
    fi
  fi
fi
fi
fi
fi
fi
fi
fi
#force_color_prompt=yes
# for examples
# for setting history length see HISTSIZE and HISTFILESIZE in bash(1)
HISTCONTROL=ignoreboth
HISTFILESIZE=2000
HISTSIZE=1000
        . "/home/aveey/miniconda3/etc/profile.d/conda.sh"
    *i*) ;;
if [ "$color_prompt" = yes ]; then
if [ $? -eq 0 ]; then
if [ -f ~/.bash_aliases ]; then
    if [ -f "/home/aveey/miniconda3/etc/profile.d/conda.sh" ]; then
  if [ -f /usr/share/bash-completion/bash_completion ]; then
if [ -n "$force_color_prompt" ]; then
# If not running interactively, don't do anything
# If set, the pattern "**" used in a pathname expansion context will
if ! shopt -oq posix; then
# If this is an xterm set the title to user@host:dir
if [ -x /usr/bin/dircolors ]; then
    if [ -x /usr/bin/tput ] && tput setaf 1 >&/dev/null; then
if [ -z "${debian_chroot:-}" ] && [ -r /etc/debian_chroot ]; then
	# (ISO/IEC-6429). (Lack of such support is extremely rare, and such
# make less more friendly for non-text input files, see lesspipe(1)
# match all files and zero or more directories and subdirectories.
# off by default to not distract the user: the focus in a terminal window
    PS1='${debian_chroot:+($debian_chroot)}\[\033[01;32m\]\u@\h\[\033[00m\]:\[\033[01;34m\]\w\[\033[00m\]\$ '
    PS1='${debian_chroot:+($debian_chroot)}\u@\h:\w\$ '
    PS1="\[\e]0;${debian_chroot:+($debian_chroot)}\u@\h: \w\a\]$PS1"
      *) return;;
# See bash(1) for more options
# See /usr/share/doc/bash-doc/examples in the bash-doc package.
# see /usr/share/doc/bash/examples/startup-files (in the package bash-doc)
# set a fancy prompt (non-color, unless we know we "want" color)
# set variable identifying the chroot you work in (used in the prompt below)
shopt -s checkwinsize
#shopt -s globstar
shopt -s histappend
# should be on the output of commands, not on the prompt
#   sleep 10; alert
# some more ls aliases
# sources /etc/bash.bashrc).
    test -r ~/.dircolors && eval "$(dircolors -b ~/.dircolors)" || eval "$(dircolors -b)"
# this, if it's already enabled in /etc/bash.bashrc and /etc/profile
# uncomment for a colored prompt, if the terminal has the capability; turned
unset color_prompt force_color_prompt
unset __conda_setup
# update the values of LINES and COLUMNS.
    . /usr/share/bash-completion/bash_completion
	# We have color support; assume it's compliant with Ecma-48
    xterm-color|*-256color) color_prompt=yes;;
xterm*|rxvt*)
[ -x /usr/bin/lesspipe ] && eval "$(SHELL=/bin/sh lesspipe)"
# You may want to put all your additions into a separate file like
--------------------------------------------------------------------------------
























    ;;
    ;;
*)
	# a case would tend to support setf rather than setaf.)
# Add an "alert" alias for long running commands.  Use like so:
alias alert='notify-send --urgency=low -i "$([ $? = 0 ] && echo terminal || echo error)" "$(history|tail -n1|sed -e '\''s/^\s*[0-9]\+\s*//;s/[;&|]\s*alert$//'\'')"'
alias cls='clear'
alias debugpp='VERBOSE=1 BATS_OPTIONS="--no-tempdir-cleanup --pretty --verbose-run --show-output-of-passing-tests" batspp'
# Alias definitions.
    #alias dir='dir --color=auto'
    alias egrep='egrep --color=auto'
    alias fgrep='fgrep --color=auto'
# ALIAS FOR BATSPP & ITS DEBUG MODE
# Alias for clearing screen
alias get-batspp="./jupyter_to_batspp.py"
    alias grep='grep --color=auto'
alias la='ls -A'
    alias ll='exa --icons -la'
alias ll='ls -alF'
alias l='ls -CF'
alias lsd='ls -d */'
    alias ls='exa --icons'
alias simple-batspp-debug='VERBOSE=1 BATS_OPTIONS="--no-tempdir-cleanup --pretty --verbose-run --show-output-of-passing-tests" simple-batspp'
alias simple-batspp="./simple_batspp.py"
#Alias to display directory
    alias tree='exa --icons --tree'
    #alias vdir='vdir --color=auto'
# append to the history file, don't overwrite it
    . ~/.bash_aliases
# ~/.bash_aliases, instead of adding them here directly.
# ~/.bashrc: executed by bash(1) for non-login shells.
case $- in
case "$TERM" in
case "$TERM" in
# check the window size after each command and, if necessary,
# colored GCC warnings and errors
	color_prompt=
	color_prompt=yes
# <<< conda initialize <<<
# >>> conda initialize >>>
__conda_setup="$('/home/aveey/miniconda3/bin/conda' 'shell.bash' 'hook' 2> /dev/null)"
# !! Contents within this block are managed by 'conda init' !!
    debian_chroot=$(cat /etc/debian_chroot)
# don't put duplicate lines or lines starting with space in the history.
  elif [ -f /etc/bash_completion ]; then
    else
    else
else
else
# enable color support of ls and also add handy aliases
# enable programmable completion features (you don't need to enable
esac
esac
esac
    . /etc/bash_completion
    eval "$__conda_setup"
#export GCC_COLORS='error=01;31:warning=01;35:note=01;36:caret=01;32:locus=01:quote=01'
        export PATH="/home/aveey/miniconda3/bin:$PATH"
    fi
    fi
  fi
fi
fi
fi
fi
fi
fi
fi
#force_color_prompt=yes
# for examples
# for setting history length see HISTSIZE and HISTFILESIZE in bash(1)
HISTCONTROL=ignoreboth
HISTFILESIZE=2000
HISTSIZE=1000
        . "/home/aveey/miniconda3/etc/profile.d/conda.sh"
    *i*) ;;
if [ "$color_prompt" = yes ]; then
if [ $? -eq 0 ]; then
if [ -f ~/.bash_aliases ]; then
    if [ -f "/home/aveey/miniconda3/etc/profile.d/conda.sh" ]; then
  if [ -f /usr/share/bash-completion/bash_completion ]; then
if [ -n "$force_color_prompt" ]; then
# If not running interactively, don't do anything
# If set, the pattern "**" used in a pathname expansion context will
if ! shopt -oq posix; then
# If this is an xterm set the title to user@host:dir
if [ -x /usr/bin/dircolors ]; then
    if [ -x /usr/bin/tput ] && tput setaf 1 >&/dev/null; then
if [ -z "${debian_chroot:-}" ] && [ -r /etc/debian_chroot ]; then
	# (ISO/IEC-6429). (Lack of such support is extremely rare, and such
# make less more friendly for non-text input files, see lesspipe(1)
# match all files and zero or more directories and subdirectories.
# off by default to not distract the user: the focus in a terminal window
    PS1='${debian_chroot:+($debian_chroot)}\[\033[01;32m\]\u@\h\[\033[00m\]:\[\033[01;34m\]\w\[\033[00m\]\$ '
    PS1='${debian_chroot:+($debian_chroot)}\u@\h:\w\$ '
    PS1="\[\e]0;${debian_chroot:+($debian_chroot)}\u@\h: \w\a\]$PS1"
      *) return;;
# See bash(1) for more options
# See /usr/share/doc/bash-doc/examples in the bash-doc package.
# see /usr/share/doc/bash/examples/startup-files (in the package bash-doc)
# set a fancy prompt (non-color, unless we know we "want" color)
# set variable identifying the chroot you work in (used in the prompt below)
shopt -s checkwinsize
#shopt -s globstar
shopt -s histappend
# should be on the output of commands, not on the prompt
#   sleep 10; alert
# some more ls aliases
# sources /etc/bash.bashrc).
    test -r ~/.dircolors && eval "$(dircolors -b ~/.dircolors)" || eval "$(dircolors -b)"
# this, if it's already enabled in /etc/bash.bashrc and /etc/profile
# uncomment for a colored prompt, if the terminal has the capability; turned
unset color_prompt force_color_prompt
unset __conda_setup
# update the values of LINES and COLUMNS.
    . /usr/share/bash-completion/bash_completion
	# We have color support; assume it's compliant with Ecma-48
    xterm-color|*-256color) color_prompt=yes;;
xterm*|rxvt*)
[ -x /usr/bin/lesspipe ] && eval "$(SHELL=/bin/sh lesspipe)"
# You may want to put all your additions into a separate file like
--------------------------------------------------------------------------------
# You may want to put all your additions into a separate file like
[ -x /usr/bin/lesspipe ] && eval "$(SHELL=/bin/sh lesspipe)"
xterm*|rxvt*)
    xterm-color|*-256color) color_prompt=yes;;
	# We have color support; assume it's compliant with Ecma-48
    . /usr/share/bash-completion/bash_completion
# update the values of LINES and COLUMNS.
unset __conda_setup
unset color_prompt force_color_prompt
# uncomment for a colored prompt, if the terminal has the capability; turned
# this, if it's already enabled in /etc/bash.bashrc and /etc/profile
    test -r ~/.dircolors && eval "$(dircolors -b ~/.dircolors)" || eval "$(dircolors -b)"
# sources /etc/bash.bashrc).
# some more ls aliases
#   sleep 10; alert
# should be on the output of commands, not on the prompt
shopt -s histappend
#shopt -s globstar
shopt -s checkwinsize
# set variable identifying the chroot you work in (used in the prompt below)
# set a fancy prompt (non-color, unless we know we "want" color)
# see /usr/share/doc/bash/examples/startup-files (in the package bash-doc)
# See /usr/share/doc/bash-doc/examples in the bash-doc package.
# See bash(1) for more options
      *) return;;
    PS1="\[\e]0;${debian_chroot:+($debian_chroot)}\u@\h: \w\a\]$PS1"
    PS1='${debian_chroot:+($debian_chroot)}\u@\h:\w\$ '
    PS1='${debian_chroot:+($debian_chroot)}\[\033[01;32m\]\u@\h\[\033[00m\]:\[\033[01;34m\]\w\[\033[00m\]\$ '
# off by default to not distract the user: the focus in a terminal window
# match all files and zero or more directories and subdirectories.
# make less more friendly for non-text input files, see lesspipe(1)
	# (ISO/IEC-6429). (Lack of such support is extremely rare, and such
if [ -z "${debian_chroot:-}" ] && [ -r /etc/debian_chroot ]; then
    if [ -x /usr/bin/tput ] && tput setaf 1 >&/dev/null; then
if [ -x /usr/bin/dircolors ]; then
# If this is an xterm set the title to user@host:dir
if ! shopt -oq posix; then
# If set, the pattern "**" used in a pathname expansion context will
# If not running interactively, don't do anything
if [ -n "$force_color_prompt" ]; then
  if [ -f /usr/share/bash-completion/bash_completion ]; then
    if [ -f "/home/aveey/miniconda3/etc/profile.d/conda.sh" ]; then
if [ -f ~/.bash_aliases ]; then
if [ $? -eq 0 ]; then
if [ "$color_prompt" = yes ]; then
    *i*) ;;
        . "/home/aveey/miniconda3/etc/profile.d/conda.sh"
HISTSIZE=1000
HISTFILESIZE=2000
HISTCONTROL=ignoreboth
# for setting history length see HISTSIZE and HISTFILESIZE in bash(1)
# for examples
#force_color_prompt=yes
fi
fi
fi
fi
fi
fi
fi
  fi
    fi
    fi
        export PATH="/home/aveey/miniconda3/bin:$PATH"
#export GCC_COLORS='error=01;31:warning=01;35:note=01;36:caret=01;32:locus=01:quote=01'
    eval "$__conda_setup"
    . /etc/bash_completion
esac
esac
esac
# enable programmable completion features (you don't need to enable
# enable color support of ls and also add handy aliases
else
else
    else
    else
  elif [ -f /etc/bash_completion ]; then
# don't put duplicate lines or lines starting with space in the history.
    debian_chroot=$(cat /etc/debian_chroot)
# !! Contents within this block are managed by 'conda init' !!
__conda_setup="$('/home/aveey/miniconda3/bin/conda' 'shell.bash' 'hook' 2> /dev/null)"
# >>> conda initialize >>>
# <<< conda initialize <<<
	color_prompt=yes
	color_prompt=
# colored GCC warnings and errors
# check the window size after each command and, if necessary,
case "$TERM" in
case "$TERM" in
case $- in
# ~/.bashrc: executed by bash(1) for non-login shells.
# ~/.bash_aliases, instead of adding them here directly.
    . ~/.bash_aliases
# append to the history file, don't overwrite it
    #alias vdir='vdir --color=auto'
    alias tree='exa --icons --tree'
#Alias to display directory
alias simple-batspp="./simple_batspp.py"
alias simple-batspp-debug='VERBOSE=1 BATS_OPTIONS="--no-tempdir-cleanup --pretty --verbose-run --show-output-of-passing-tests" simple-batspp'
    alias ls='exa --icons'
alias lsd='ls -d */'
alias l='ls -CF'
alias ll='ls -alF'
    alias ll='exa --icons -la'
alias la='ls -A'
    alias grep='grep --color=auto'
alias get-batspp="./jupyter_to_batspp.py"
# Alias for clearing screen
# ALIAS FOR BATSPP & ITS DEBUG MODE
    alias fgrep='fgrep --color=auto'
    alias egrep='egrep --color=auto'
    #alias dir='dir --color=auto'
# Alias definitions.
alias debugpp='VERBOSE=1 BATS_OPTIONS="--no-tempdir-cleanup --pretty --verbose-run --show-output-of-passing-tests" batspp'
alias cls='clear'
alias alert='notify-send --urgency=low -i "$([ $? = 0 ] && echo terminal || echo error)" "$(history|tail -n1|sed -e '\''s/^\s*[0-9]\+\s*//;s/[;&|]\s*alert$//'\'')"'
# Add an "alert" alias for long running commands.  Use like so:
	# a case would tend to support setf rather than setaf.)
*)
    ;;
    ;;
























--------------------------------------------------------------------------------
# You may want to put all your additions into a separate file like
[ -x /usr/bin/lesspipe ] && eval "$(SHELL=/bin/sh lesspipe)"
xterm*|rxvt*)
    xterm-color|*-256color) color_prompt=yes;;
	# We have color support; assume it's compliant with Ecma-48
    . /usr/share/bash-completion/bash_completion
# update the values of LINES and COLUMNS.
unset __conda_setup
unset color_prompt force_color_prompt
# uncomment for a colored prompt, if the terminal has the capability; turned
# this, if it's already enabled in /etc/bash.bashrc and /etc/profile
    test -r ~/.dircolors && eval "$(dircolors -b ~/.dircolors)" || eval "$(dircolors -b)"
# sources /etc/bash.bashrc).
# some more ls aliases
#   sleep 10; alert
# should be on the output of commands, not on the prompt
shopt -s histappend
#shopt -s globstar
shopt -s checkwinsize
# set variable identifying the chroot you work in (used in the prompt below)
# set a fancy prompt (non-color, unless we know we "want" color)
# see /usr/share/doc/bash/examples/startup-files (in the package bash-doc)
# See /usr/share/doc/bash-doc/examples in the bash-doc package.
# See bash(1) for more options
      *) return;;
    PS1="\[\e]0;${debian_chroot:+($debian_chroot)}\u@\h: \w\a\]$PS1"
    PS1='${debian_chroot:+($debian_chroot)}\u@\h:\w\$ '
    PS1='${debian_chroot:+($debian_chroot)}\[\033[01;32m\]\u@\h\[\033[00m\]:\[\033[01;34m\]\w\[\033[00m\]\$ '
# off by default to not distract the user: the focus in a terminal window
# match all files and zero or more directories and subdirectories.
# make less more friendly for non-text input files, see lesspipe(1)
	# (ISO/IEC-6429). (Lack of such support is extremely rare, and such
if [ -z "${debian_chroot:-}" ] && [ -r /etc/debian_chroot ]; then
    if [ -x /usr/bin/tput ] && tput setaf 1 >&/dev/null; then
if [ -x /usr/bin/dircolors ]; then
# If this is an xterm set the title to user@host:dir
if ! shopt -oq posix; then
# If set, the pattern "**" used in a pathname expansion context will
# If not running interactively, don't do anything
if [ -n "$force_color_prompt" ]; then
  if [ -f /usr/share/bash-completion/bash_completion ]; then
    if [ -f "/home/aveey/miniconda3/etc/profile.d/conda.sh" ]; then
if [ -f ~/.bash_aliases ]; then
if [ $? -eq 0 ]; then
if [ "$color_prompt" = yes ]; then
    *i*) ;;
        . "/home/aveey/miniconda3/etc/profile.d/conda.sh"
HISTSIZE=1000
HISTFILESIZE=2000
HISTCONTROL=ignoreboth
# for setting history length see HISTSIZE and HISTFILESIZE in bash(1)
# for examples
#force_color_prompt=yes
fi
fi
fi
fi
fi
fi
fi
  fi
    fi
    fi
        export PATH="/home/aveey/miniconda3/bin:$PATH"
#export GCC_COLORS='error=01;31:warning=01;35:note=01;36:caret=01;32:locus=01:quote=01'
    eval "$__conda_setup"
    . /etc/bash_completion
esac
esac
esac
# enable programmable completion features (you don't need to enable
# enable color support of ls and also add handy aliases
else
else
    else
    else
  elif [ -f /etc/bash_completion ]; then
# don't put duplicate lines or lines starting with space in the history.
    debian_chroot=$(cat /etc/debian_chroot)
# !! Contents within this block are managed by 'conda init' !!
__conda_setup="$('/home/aveey/miniconda3/bin/conda' 'shell.bash' 'hook' 2> /dev/null)"
# >>> conda initialize >>>
# <<< conda initialize <<<
	color_prompt=yes
	color_prompt=
# colored GCC warnings and errors
# check the window size after each command and, if necessary,
case "$TERM" in
case "$TERM" in
case $- in
# ~/.bashrc: executed by bash(1) for non-login shells.
# ~/.bash_aliases, instead of adding them here directly.
    . ~/.bash_aliases
# append to the history file, don't overwrite it
    #alias vdir='vdir --color=auto'
    alias tree='exa --icons --tree'
#Alias to display directory
alias simple-batspp="./simple_batspp.py"
alias simple-batspp-debug='VERBOSE=1 BATS_OPTIONS="--no-tempdir-cleanup --pretty --verbose-run --show-output-of-passing-tests" simple-batspp'
    alias ls='exa --icons'
alias lsd='ls -d */'
alias l='ls -CF'
alias ll='ls -alF'
    alias ll='exa --icons -la'
alias la='ls -A'
    alias grep='grep --color=auto'
alias get-batspp="./jupyter_to_batspp.py"
# Alias for clearing screen
# ALIAS FOR BATSPP & ITS DEBUG MODE
    alias fgrep='fgrep --color=auto'
    alias egrep='egrep --color=auto'
    #alias dir='dir --color=auto'
# Alias definitions.
alias debugpp='VERBOSE=1 BATS_OPTIONS="--no-tempdir-cleanup --pretty --verbose-run --show-output-of-passing-tests" batspp'
alias cls='clear'
alias alert='notify-send --urgency=low -i "$([ $? = 0 ] && echo terminal || echo error)" "$(history|tail -n1|sed -e '\''s/^\s*[0-9]\+\s*//;s/[;&|]\s*alert$//'\'')"'
# Add an "alert" alias for long running commands.  Use like so:
	# a case would tend to support setf rather than setaf.)
*)
    ;;
    ;;
























--------------------------------------------------------------------------------

# ECHOES THE FILE 
$ cat ~/.bashrc | echoize
# ~/.bashrc: executed by bash(1) for non-login shells. # see /usr/share/doc/bash/examples/startup-files (in the package bash-doc) # for examples

# If not running interactively, don't do anything case $- in     *i*) ;;       *) return;; esac

# don't put duplicate lines or lines starting with space in the history. # See bash(1) for more options HISTCONTROL=ignoreboth

# append to the history file, don't overwrite it shopt -s histappend

# for setting history length see HISTSIZE and HISTFILESIZE in bash(1) HISTSIZE=1000 HISTFILESIZE=2000

# check the window size after each command and, if necessary, # update the values of LINES and COLUMNS. shopt -s checkwinsize

# If set, the pattern "**" used in a pathname expansion context will # match all files and zero or more directories and subdirectories. #shopt -s globstar

# make less more friendly for non-text input files, see lesspipe(1) [ -x /usr/bin/lesspipe ] && eval "$(SHELL=/bin/sh lesspipe)"

# set variable identifying the chroot you work in (used in the prompt below) if [ -z "${debian_chroot:-}" ] && [ -r /etc/debian_chroot ]; then     debian_chroot=$(cat /etc/debian_chroot) fi

# set a fancy prompt (non-color, unless we know we "want" color) case "$TERM" in     xterm-color|*-256color) color_prompt=yes;; esac

# uncomment for a colored prompt, if the terminal has the capability; turned # off by default to not distract the user: the focus in a terminal window # should be on the output of commands, not on the prompt #force_color_prompt=yes

if [ -n "$force_color_prompt" ]; then     if [ -x /usr/bin/tput ] && tput setaf 1 >&/dev/null; then 	# We have color support; assume it's compliant with Ecma-48 	# (ISO/IEC-6429). (Lack of such support is extremely rare, and such 	# a case would tend to support setf rather than setaf.) 	color_prompt=yes     else 	color_prompt=     fi fi

if [ "$color_prompt" = yes ]; then     PS1='${debian_chroot:+($debian_chroot)}\[\033[01;32m\]\u@\h\[\033[00m\]:\[\033[01;34m\]\w\[\033[00m\]\$ ' else     PS1='${debian_chroot:+($debian_chroot)}\u@\h:\w\$ ' fi unset color_prompt force_color_prompt

# If this is an xterm set the title to user@host:dir case "$TERM" in xterm*|rxvt*)     PS1="\[\e]0;${debian_chroot:+($debian_chroot)}\u@\h: \w\a\]$PS1"     ;; *)     ;; esac

# enable color support of ls and also add handy aliases if [ -x /usr/bin/dircolors ]; then     test -r ~/.dircolors && eval "$(dircolors -b ~/.dircolors)" || eval "$(dircolors -b)"     alias ls='exa --icons'     alias ll='exa --icons -la'     alias tree='exa --icons --tree'     #alias dir='dir --color=auto'     #alias vdir='vdir --color=auto'

    alias grep='grep --color=auto'     alias fgrep='fgrep --color=auto'     alias egrep='egrep --color=auto' fi

# colored GCC warnings and errors #export GCC_COLORS='error=01;31:warning=01;35:note=01;36:caret=01;32:locus=01:quote=01'

# some more ls aliases alias ll='ls -alF' alias la='ls -A' alias l='ls -CF'

# Add an "alert" alias for long running commands.  Use like so: #   sleep 10; alert alias alert='notify-send --urgency=low -i "$([ $? = 0 ] && echo terminal || echo error)" "$(history|tail -n1|sed -e '\''s/^\s*[0-9]\+\s*//;s/[;&|]\s*alert$//'\'')"'

# Alias definitions. # You may want to put all your additions into a separate file like # ~/.bash_aliases, instead of adding them here directly. # See /usr/share/doc/bash-doc/examples in the bash-doc package.

if [ -f ~/.bash_aliases ]; then     . ~/.bash_aliases fi

# enable programmable completion features (you don't need to enable # this, if it's already enabled in /etc/bash.bashrc and /etc/profile # sources /etc/bash.bashrc). if ! shopt -oq posix; then   if [ -f /usr/share/bash-completion/bash_completion ]; then     . /usr/share/bash-completion/bash_completion   elif [ -f /etc/bash_completion ]; then     . /etc/bash_completion   fi fi # Alias for clearing screen alias cls='clear'

#Alias to display directory alias lsd='ls -d */'

# >>> conda initialize >>> # !! Contents within this block are managed by 'conda init' !! __conda_setup="$('/home/aveey/miniconda3/bin/conda' 'shell.bash' 'hook' 2> /dev/null)" if [ $? -eq 0 ]; then     eval "$__conda_setup" else     if [ -f "/home/aveey/miniconda3/etc/profile.d/conda.sh" ]; then         . "/home/aveey/miniconda3/etc/profile.d/conda.sh"     else         export PATH="/home/aveey/miniconda3/bin:$PATH"     fi fi unset __conda_setup # <<< conda initialize <<<

# ALIAS FOR BATSPP & ITS DEBUG MODE alias get-batspp="./jupyter_to_batspp.py" alias simple-batspp="./simple_batspp.py" alias simple-batspp-debug='VERBOSE=1 BATS_OPTIONS="--no-tempdir-cleanup --pretty --verbose-run --show-output-of-passing-tests" simple-batspp' alias debugpp='VERBOSE=1 BATS_OPTIONS="--no-tempdir-cleanup --pretty --verbose-run --show-output-of-passing-tests" batspp'

# SORTS BY PARAGRAPH
$ cat ~/.bashrc | para-sort
    alias grep='grep --color=auto'
    alias fgrep='fgrep --color=auto'
    alias egrep='egrep --color=auto'
fi


# >>> conda initialize >>>
# !! Contents within this block are managed by 'conda init' !!
__conda_setup="$('/home/aveey/miniconda3/bin/conda' 'shell.bash' 'hook' 2> /dev/null)"
if [ $? -eq 0 ]; then
    eval "$__conda_setup"
else
    if [ -f "/home/aveey/miniconda3/etc/profile.d/conda.sh" ]; then
        . "/home/aveey/miniconda3/etc/profile.d/conda.sh"
    else
        export PATH="/home/aveey/miniconda3/bin:$PATH"
    fi
fi
unset __conda_setup
# <<< conda initialize <<<


# ALIAS FOR BATSPP & ITS DEBUG MODE
alias get-batspp="./jupyter_to_batspp.py"
alias simple-batspp="./simple_batspp.py"
alias simple-batspp-debug='VERBOSE=1 BATS_OPTIONS="--no-tempdir-cleanup --pretty --verbose-run --show-output-of-passing-tests" simple-batspp'
alias debugpp='VERBOSE=1 BATS_OPTIONS="--no-tempdir-cleanup --pretty --verbose-run --show-output-of-passing-tests" batspp'

# Add an "alert" alias for long running commands.  Use like so:
#   sleep 10; alert
alias alert='notify-send --urgency=low -i "$([ $? = 0 ] && echo terminal || echo error)" "$(history|tail -n1|sed -e '\''s/^\s*[0-9]\+\s*//;s/[;&|]\s*alert$//'\'')"'


# Alias definitions.
# You may want to put all your additions into a separate file like
# ~/.bash_aliases, instead of adding them here directly.
# See /usr/share/doc/bash-doc/examples in the bash-doc package.


# If not running interactively, don't do anything
case $- in
    *i*) ;;
      *) return;;
esac


# If set, the pattern "**" used in a pathname expansion context will
# match all files and zero or more directories and subdirectories.
#shopt -s globstar


# If this is an xterm set the title to user@host:dir
case "$TERM" in
xterm*|rxvt*)
    PS1="\[\e]0;${debian_chroot:+($debian_chroot)}\u@\h: \w\a\]$PS1"
    ;;
*)
    ;;
esac


# append to the history file, don't overwrite it
shopt -s histappend


# check the window size after each command and, if necessary,
# update the values of LINES and COLUMNS.
shopt -s checkwinsize


# colored GCC warnings and errors
#export GCC_COLORS='error=01;31:warning=01;35:note=01;36:caret=01;32:locus=01:quote=01'


# don't put duplicate lines or lines starting with space in the history.
# See bash(1) for more options
HISTCONTROL=ignoreboth


# enable color support of ls and also add handy aliases
if [ -x /usr/bin/dircolors ]; then
    test -r ~/.dircolors && eval "$(dircolors -b ~/.dircolors)" || eval "$(dircolors -b)"
    alias ls='exa --icons'
    alias ll='exa --icons -la'
    alias tree='exa --icons --tree'
    #alias dir='dir --color=auto'
    #alias vdir='vdir --color=auto'


# enable programmable completion features (you don't need to enable
# this, if it's already enabled in /etc/bash.bashrc and /etc/profile
# sources /etc/bash.bashrc).
if ! shopt -oq posix; then
  if [ -f /usr/share/bash-completion/bash_completion ]; then
    . /usr/share/bash-completion/bash_completion
  elif [ -f /etc/bash_completion ]; then
    . /etc/bash_completion
  fi
fi
# Alias for clearing screen
alias cls='clear'


# for setting history length see HISTSIZE and HISTFILESIZE in bash(1)
HISTSIZE=1000
HISTFILESIZE=2000


# make less more friendly for non-text input files, see lesspipe(1)
[ -x /usr/bin/lesspipe ] && eval "$(SHELL=/bin/sh lesspipe)"


# set a fancy prompt (non-color, unless we know we "want" color)
case "$TERM" in
    xterm-color|*-256color) color_prompt=yes;;
esac


# set variable identifying the chroot you work in (used in the prompt below)
if [ -z "${debian_chroot:-}" ] && [ -r /etc/debian_chroot ]; then
    debian_chroot=$(cat /etc/debian_chroot)
fi


# some more ls aliases
alias ll='ls -alF'
alias la='ls -A'
alias l='ls -CF'


# uncomment for a colored prompt, if the terminal has the capability; turned
# off by default to not distract the user: the focus in a terminal window
# should be on the output of commands, not on the prompt
#force_color_prompt=yes


# ~/.bashrc: executed by bash(1) for non-login shells.
# see /usr/share/doc/bash/examples/startup-files (in the package bash-doc)
# for examples


#Alias to display directory
alias lsd='ls -d */'


if [ "$color_prompt" = yes ]; then
    PS1='${debian_chroot:+($debian_chroot)}\[\033[01;32m\]\u@\h\[\033[00m\]:\[\033[01;34m\]\w\[\033[00m\]\$ '
else
    PS1='${debian_chroot:+($debian_chroot)}\u@\h:\w\$ '
fi
unset color_prompt force_color_prompt


if [ -f ~/.bash_aliases ]; then
    . ~/.bash_aliases
fi


if [ -n "$force_color_prompt" ]; then
    if [ -x /usr/bin/tput ] && tput setaf 1 >&/dev/null; then
	# We have color support; assume it's compliant with Ecma-48
	# (ISO/IEC-6429). (Lack of such support is extremely rare, and such
	# a case would tend to support setf rather than setaf.)
	color_prompt=yes
    else
	color_prompt=
    fi
fi
