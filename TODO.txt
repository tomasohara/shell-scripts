Things to do

- Check for duplicate code such as inadvertent merges artifacts. This is
  probably more of an issue with aliases (e.g., tomohara-aliases.bash).
- Add simple example-based tests for common scripts (see simple_batspp.py).
- check for stupid Perl argument unpacking bugs (e.g., long-standing one in common.perl)
    sub debug_on {
      ## BAD: my($level) = $_;
      my($level) = @_;
      ...
      }
- Prune temporary scripts (e.g., it.perl).
- P2: Clarify when all-tomohara-settings needed.
- P3: Document environment variable usage in main scripts.
- P4: Modernize Bash expressions in scripts (e.g., [ $x = $y ] => [ $x == $y ]).
- P3: remove old variations of scripts
  $ ls archive/*telnet*
  archive/bad-telnet.perl  archive/telnet.perl  archive/test-telnet.perl
  archive/new-telnet.perl  archive/telnet.sh    archive/was-telnet.perl
- P4: make sure shell scripts honor --help

--------------------------------------------------------------------------------
Tue 21 Apr 26

note:
- should  fix most-recent-backup function to avoid issue with dot file globbing
- suggestion from POE Assistant
- old
   $LS -t "$dir"/* "$dir"/.* | $EGREP "/$file(~|.~.*)?$" | head -1
- new
    shopt -s dotglob nullglob
    local files=( "$dir"/* )
    shopt -u dotglob nullglob

    printf '%s\n' "${files[@]}" \
        | $EGREP "/$file(~|.~.*)?$" \
        | xargs ls -t 2>/dev/null \
        | head -1

--------------------------------------------------------------------------------
Sat 25 Apr 26

note:
- P4: reword usages of maldito
- ex: "maldito shellcheck" => "shellcheck filtering

--------------------------------------------------------------------------------
Sun 12 Jul 26

note:
- P2: rename core alias-related scripts using tomohara affix to make easier to find (e.g., tomohara-startup-tracing.bash).

--------------------------------------------------------------------------------
Fri 24 Jul 26 [19:52:26]

P2: run scripts under strict error checking (e.g., `set -u` might error on $1 checks)
P3: address TODO notes in new docs dir (e.g., todo-address-alias-sanity-check-jul2026.pdf)
